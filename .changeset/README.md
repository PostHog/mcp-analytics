# Changesets

This repo uses Changesets for versioning, following the same release shape as the PostHog JavaScript SDKs.

For user-visible changes, add a changeset with:

```bash
pnpm changeset
```

Version packages with:

```bash
pnpm bump
```
